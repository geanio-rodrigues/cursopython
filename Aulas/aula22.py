# Importando os módulos de outro local no próprio computador.
from uteis1 import fatorial

num = int(input('Digite um valor: '))
fat = fatorial(num)
print(f'O fatorial de {num} é {fat}')

# Prática ↓
# Pasta Modulos na parte de baixo
# Contém a parte prática dessa aula.